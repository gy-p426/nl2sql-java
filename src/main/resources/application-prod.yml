spring:
  profiles:
    active: prod

logging:
  level:
    root: WARN
    com.nl2sql: INFO
  file:
    name: logs/nl2sql-prod.log
    max-size: 100MB
    max-history: 30

server:
  port: 8080
  compression:
    enabled: true
  tomcat:
    max-threads: 200
    min-spare-threads: 10
